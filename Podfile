# Uncomment the next line to define a global platform for your project
platform :ios, '11.0'

target 'EasynvestSimulator' do
  use_frameworks!

  # Pods for EasynvestSimulator
  pod 'SwiftLint'
  pod 'Embassy', '~> 4.0'
  pod 'Instabug'
  
  target 'EasynvestSimulatorTests' do
      inherit! :complete
  end
  
end
